
# ASP.NET Core Filtre ve Middleware Uygulamasý

Bu proje, ASP.NET Core kullanarak filtreler ve middleware'lerin nasýl kullanýlacaðýný göstermek için oluþturulmuþtur. 

## Baþlarken

Bu adýmlarý takip ederek projeyi yerel makinenizde çalýþtýrabilirsiniz.

### Gereksinimler

Bu projeyi çalýþtýrmak için aþaðýdaki yazýlýmlara ihtiyacýnýz olacaktýr:

- .NET 5 SDK veya daha üstü

### Kurulum

1. Bu depoyu yerel makinenize klonlayýn:

```
git clone https://github.com/sizin-kullanýcý-adýnýz/ASPNET-Core-Filtre-Middleware-Uygulamasý.git
```

2. Proje dizinine gidin:

```
cd ASPNET-Core-Filtre-Middleware-Uygulamasý
```

3. Proje dosyalarýný yerel ortamýnýza yüklemek için aþaðýdaki komutu çalýþtýrýn:

```
dotnet restore
```

4. Projeyi çalýþtýrýn:

```
dotnet run
```

## Kullaným

Bu proje, ASP.NET Core filtrelerini ve middleware'lerini anlamanýza yardýmcý olmak için örnekler içermektedir. Kod içerisindeki yorum satýrlarý ve belgeler size yol gösterecektir.

## Katkýda Bulunma

1. Bu depoyu fork edin (https://github.com/sizin-kullanýcý-adýnýz/ASPNET-Core-Filtre-Middleware-Uygulamasý/fork)
2. Özellik dalý oluþturun (`git checkout -b özellik/YeniÖzellik`)
3. Deðiþikliklerinizi yapýn
4. Deðiþikliklerinizi kaydedin (`git commit -am 'Yeni özellik ekledi'`)
5. Dalýnýza itin (`git push origin özellik/YeniÖzellik`)
6. Bir Pull Request açýn



